# DataSource settings

spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Logging settings
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# JWT settings
security.jwt.secret=dfiho4358ohdof3498werjoi
security.jwt.issuer=Main
security.jwt.ttlMillis=86400000

#URL para sobreescribir al localhost
prod.url={URL_PROD}

#JWT
# Configuracion de JWT
security.jwt.private.key=bb86dcc56ea7933e237f725e664d083bb3c2ff227f1d93d58d58792d06b816c3
security.jwt.user.generator=GRUPO_1

server.port=8080